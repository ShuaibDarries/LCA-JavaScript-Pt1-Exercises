// Empty array called colors
let colors = [];

// Added 3 different colors to the colors array
colors.push("red");
colors.push("blue");
colors.push("green");

// Array called numbers
let numbers = [1, 2, 3, 4, 5];

// Remove the last color from the colors array
colors.pop();

// adding a new color to the beginning of the array
colors.unshift("yellow");

// Length of both arrays
let colorsLength = colors.length;
let numbersLength = numbers.length;
